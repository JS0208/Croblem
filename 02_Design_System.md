# 디자인 시스템: 노션/애플 스타일 모던 미니멀리즘

## 1. 레이아웃 & 타이포그래피 (가독성 최우선)
- **최대 너비(Max-width):** 720px로 고정하여 화면 중앙에 배치. PC 환경에서도 글줄이 너무 길어지지 않게 통제.
- **메인 폰트:** `Pretendard` 웹 폰트 적용. 문제 번호 등에는 `Toss Product Sans` 또는 `Gmarket Sans` 적용 가능.
- **텍스트 리듬:** - 기본 줄 간격(line-height): 1.6 ~ 1.8
  - 자간(letter-spacing): -0.02em
  - 문제 내 '도입/조건/질문' 텍스트의 위계를 CSS 클래스로 명확히 구분할 것 (예: 조건은 약간 옅은 색과 들여쓰기 적용).

## 2. 컬러 시스템 (Color Palette)
- **배경 (Background):** `Apple 쿨그레이 (#F5F5F7)` - 극도로 밝은 회색.
- **카드 배경 (Card):** `#FFFFFF` - 완벽한 흰색으로 배경과 대비.
- **메인 텍스트:** `#1D1D1F` (순수 검정 지양, 애플 특유의 다크 그레이).
- **보조 텍스트 (조건, 부가설명):** `#86868B` (가독성을 해치지 않는 선의 회색).
- **포인트/액션 컬러:** 차분한 무채색 톤업 블러(예: `#0071E3` 계열의 애플 블루 또는 차분한 흑백 반전 버튼).

## 3. UI 컴포넌트 & 마이크로 인터랙션
- **문제 카드 (Card):** - 모서리 둥글기(border-radius): 16px ~ 24px
  - 그림자(Box-shadow): 매우 넓고 흐릿한 아주 연한 그림자(예: rgba(0,0,0,0.04) 기반)를 사용하여 공중에 살짝 뜬 종이 느낌 부여.
- **버튼 및 인터랙션:**
  - 버튼 Hover 시 배경색이나 투명도가 0.2초(ease-in-out)에 걸쳐 부드럽게 변하도록 전환 효과 적용.
  - '이 문제를 푼 사람' 아코디언 메뉴는 딱딱하게 나타나지 않고, 높이(height)와 투명도(opacity)가 부드럽게 트랜지션 되며 밀려 내려오도록 애니메이션 처리.