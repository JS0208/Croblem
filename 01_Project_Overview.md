# 프로젝트 개요: 텍스트 기반 미니멀 문제방 웹사이트

## 1. 목적 및 성격
- 심플하게 텍스트로만 구성된 문제들을 모아두는 정적 웹사이트.
- 사용자가 오직 문제 해결에만 집중할 수 있는 군더더기 없는 환경 제공.

## 2. 핵심 기능 및 유저 플로우
1. **문제 열람:** 사용자는 스크롤을 내리며 여러 문제 카드를 확인한다.
2. **정답자 확인:** 각 문제 하단에 위치한 '이 문제를 푼 사람' 버튼을 클릭하면, 아코디언(Accordion) UI가 아래로 부드럽게 펼쳐지며 정답자 목록(텍스트)이 나타난다.
3. **정답 제출 (카카오톡 연동):** - 각 문제 카드에는 '정답 제출하기' 버튼이 존재한다.
   - 버튼 클릭 시 클립보드에 "[문제 {ID}] 정답 제출합니다: " 텍스트가 자동으로 복사된다.
   - 복사 직후, 관리자가 지정한 카카오톡 오픈채팅(또는 1:1 오픈프로필) 링크가 새 창으로 열린다.
   - 사용자는 카톡창에 텍스트를 붙여넣기 하고, 경우의 수를 모두 해봤다는 증거 사진이나 정답을 전송한다.

## 3. 개발 원칙
- 복잡한 백엔드 없이 HTML, CSS, Vanilla JS(또는 매우 가벼운 프레임워크)만으로 구성.
- 유지보수 극대화를 위해 UI(HTML)와 데이터(JSON)를 완벽히 분리.