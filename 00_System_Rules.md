# 00. AI 시스템 규칙 및 작업 지침 (System Rules)

## 1. 최우선 방어 원칙 (절대 엄수)
- **임의 수정 금지:** 사용자의 명시적인 지시 없이 기존에 정상 작동하는 코드를 자의적으로 삭제하거나 전체 리팩토링(Refactoring)하지 마라. 
- **질문 의무화:** 명세서에 없는 예외 상황이나 누락된 기획이 발견되면, 임의로 코드를 지어내지 말고 즉시 작업을 멈춘 뒤 사용자에게 질문하여 방향을 결정받아라.
- **기억력 한계 인지:** 한 번에 너무 많은 파일이나 수백 줄의 코드를 수정하려 하지 말고, 작은 단위로 나누어 작업하고 결과를 보고해라.

## 2. 관심사의 완벽한 분리 (Separation of Concerns)
- **파일 독립성:** HTML(구조), CSS(스타일), JavaScript(동적 렌더링), JSON(데이터)은 물리적으로 완벽히 분리된 별도의 파일로 작성하라.
- **인라인 스타일 금지:** HTML 태그 내부에 직접 스타일을 지정하는 `style="..."` 속성이나 문서 내 `<style>` 태그 사용을 엄격히 금지한다.
- **데이터 하드코딩 금지:** 문제의 텍스트, 정답자 목록 등은 절대 HTML이나 JS 코드 내부에 직접 적어 넣지 마라. 모든 데이터는 반드시 `problems.json`에서 불러와야 한다.

## 3. 디자인 통제
- **명세 엄수:** `02_Design_System.md`에 명시된 레이아웃, 컬러 팔레트, 폰트 규격을 정확히 따르라. 지정되지 않은 색상이나 그림자 효과를 임의로 창조하여 적용하지 마라.
- **단순함 유지:** 화려한 애니메이션이나 불필요한 시각적 장식을 배제하고, 텍스트 가독성을 최우선으로 하라.

## 4. 코드 품질 및 유지보수
- **순수 기술 지향:** 불필요한 외부 라이브러리(jQuery, Bootstrap 등)나 무거운 프레임워크를 절대 도입하지 마라. 오직 순수 HTML, CSS, Vanilla JS만 사용해라.
- **한국어 주석:** 로직의 핵심이 되는 부분(데이터를 불러오는 곳, 정답 제출 버튼을 누를 때의 동작 등)에는 반드시 인간 관리자가 읽고 즉시 이해할 수 있도록 한국어로 상세한 주석을 달아라.